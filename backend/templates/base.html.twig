<!doctype html>
<html lang="{{ app.request.locale }}" class="h-full" style="background-color: var(--color-surface);">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}MXO Track{% endblock %}</title>
  <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
  <link rel="icon" type="image/png" href="{{ asset('favicon.png') }}">

  {# Preconnect to external CDNs for faster resource loading #}
  <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>

  <style>[x-cloak] { display: none !important; }</style>

  {# Tailwind CSS is now fully compiled by Vite (v4) — no CDN needed.
     Brand colors + Twig template utilities are included via @source + @theme in index.css. #}

  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.8/dist/cdn.min.js"></script>
  {# React App Shell CSS (theme variables, shared Tailwind utilities) #}
  {% for href in vite_entry_styles('app-shell-widget') %}
  <link rel="stylesheet" href="{{ href }}">
  {% endfor %}
  {% block styles %}{% endblock %}
</head>
<body class="h-full" {% block body_attrs %}{% endblock %}>
{% block body %}
<div class="flex flex-col h-screen w-full" style="background-color: var(--color-surface); color: var(--color-text-primary);">

  {# ── React App Shell (unified TopBar + Sidebar, same as SPA) ──── #}
  <div id="react-shell-root"></div>
  <script type="module" src="{{ vite_entry_script('app-shell-widget') }}"></script>

  {# ── Mercure URL for React notification hook ──────────────────── #}
  {% if app.user %}
  <div data-mercure-url="{{ mercure_public_url }}" style="display:none"></div>
  {% endif %}

  {# ── Main content area (matches AppLayout's flex-1 structure) ──── #}
  <div class="flex-1 overflow-auto">

    {# ── Flash messages (toasts) ─────────────────────────────────── #}
    <div class="fixed top-20 right-4 z-[60] flex flex-col gap-3 w-96 max-w-[calc(100vw-2rem)]">
      {% for type, messages in app.flashes %}
        {% for message in messages %}
          <div x-data="{ show: true }"
               x-init="setTimeout(() => show = false, 5000)"
               x-show="show"
               x-transition:enter="transition ease-out duration-300"
               x-transition:enter-start="opacity-0 translate-x-8"
               x-transition:enter-end="opacity-100 translate-x-0"
               x-transition:leave="transition ease-in duration-200"
               x-transition:leave-start="opacity-100 translate-x-0"
               x-transition:leave-end="opacity-0 translate-x-8"
               class="pointer-events-auto rounded-lg p-4 shadow-lg ring-1 ring-black/5
                      {% if type == 'success' %}bg-emerald-50 text-emerald-800 ring-emerald-200{% endif %}
                      {% if type == 'error' or type == 'danger' %}bg-red-50 text-red-800 ring-red-200{% endif %}
                      {% if type == 'warning' %}bg-amber-50 text-amber-800 ring-amber-200{% endif %}
                      {% if type == 'info' %}bg-blue-50 text-blue-800 ring-blue-200{% endif %}">
            <div class="flex items-start gap-3">
              <div class="shrink-0 pt-0.5">
                {% if type == 'success' %}
                  <svg class="h-5 w-5 text-emerald-500" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd"/>
                  </svg>
                {% elseif type == 'error' or type == 'danger' %}
                  <svg class="h-5 w-5 text-red-500" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd"/>
                  </svg>
                {% elseif type == 'warning' %}
                  <svg class="h-5 w-5 text-amber-500" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M8.485 2.495c.673-1.167 2.357-1.167 3.03 0l6.28 10.875c.673 1.167-.17 2.625-1.516 2.625H3.72c-1.347 0-2.189-1.458-1.515-2.625L8.485 2.495zM10 5a.75.75 0 01.75.75v3.5a.75.75 0 01-1.5 0v-3.5A.75.75 0 0110 5zm0 9a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd"/>
                  </svg>
                {% else %}
                  <svg class="h-5 w-5 text-blue-500" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a.75.75 0 000 1.5h.253a.25.25 0 01.244.304l-.459 2.066A1.75 1.75 0 0010.747 15H11a.75.75 0 000-1.5h-.253a.25.25 0 01-.244-.304l.459-2.066A1.75 1.75 0 009.253 9H9z" clip-rule="evenodd"/>
                  </svg>
                {% endif %}
              </div>
              <p class="text-sm font-medium flex-1">{{ message }}</p>
              <button @click="show = false" class="shrink-0 text-current opacity-50 hover:opacity-100 transition-opacity">
                <svg class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
                  <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z"/>
                </svg>
              </button>
            </div>
          </div>
        {% endfor %}
      {% endfor %}
    </div>

    {# ── Page content ────────────────────────────────────────────── #}
    <main class="py-8">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        {% block content %}{% endblock %}
      </div>
    </main>
  </div>

</div>
{% endblock %}
{% block scripts %}{% endblock %}
</body>
</html>
