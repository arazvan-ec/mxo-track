# Provider Overrides: transporte-tracking
# Generated by /workflows:discover --setup on 2026-02-19
# Overrides auto-detected provider settings from core/providers.yaml

# Configuration based on setup choices:
#   Execution mode: agent-executes
#   Review intensity: strict
#   Team size: solo

execution:
  mode: "agent-executes"
  pause_at_checkpoints: false
  auto_correct: true

review:
  intensity: "strict"
  agents:
    - "code-reviewer"
    - "architecture-reviewer"
    - "security-reviewer"
    - "performance-reviewer"
  solid_compliance: "required"

context:
  fork_heavy_skills: true
  max_session_messages: 50
  auto_compact_pct: 50

parallelization:
  strategy: "auto"
