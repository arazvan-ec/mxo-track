# Auto-generated by /workflows:discover --setup on 2026-02-19
# Manual edits are preserved on refresh

project:
  name: "transporte-tracking"
  type: "web-app"
  description: "Logistics tracking portal with GPS vehicle tracking, delivery routes, and proof-of-delivery"

backend:
  framework: "symfony"
  language: "php"
  path: "backend/"
  version: "7.4"
  php_version: "8.4"

frontend:
  framework: "twig"
  language: "php"
  path: "backend/templates/"
  js_framework: "stimulus"
  live_updates: "turbo"

database:
  type: "postgresql"
  version: "16"
  orm: "doctrine"
  orm_version: "3.x"

architecture:
  pattern: "mvc"
  multi_tenant: true
  tenant_strategy: "doctrine_sql_filter"
  identity_pattern: "dual_id"
  layers:
    - name: entity
      path: backend/src/Entity
    - name: repository
      path: backend/src/Repository
    - name: service
      path: backend/src/Service
    - name: controller
      path: backend/src/Controller
    - name: command
      path: backend/src/Command

testing:
  framework: "phpunit"
  coverage_target: 80
  locations:
    unit: "backend/tests/Unit/"
    integration: "backend/tests/Integration/"
    e2e: "scripts/"

conventions:
  commits: "conventional"
  branches: "feature-branches"
  file_naming: "match-class-name"
  strict_types: true
  doctrine_mapping: "attributes"

ai_analysis:
  last_scan: "2026-02-19"
  complexity: "medium-high"
  detected_patterns:
    - "PublicIdTrait (dual identity)"
    - "CustomerTenantFilter (multi-tenant)"
    - "Event-based lifecycle (ShipmentEvent)"
    - "Idempotency key (DriverAction)"
    - "Checkpoint pattern (VehicleCheckpoint)"
    - "Materialized last position (VehicleLastPosition)"
  reference_files:
    entity: "backend/src/Entity/Vehicle.php"
    service: "backend/src/Service/TraccarApiClient.php"
    controller: "backend/src/Controller/DriverApiController.php"
    dto: "backend/src/Dto/Driver/DeliverStopInput.php"
    enum: "backend/src/Enum/UserRole.php"

workflow:
  execution_mode: "agent-executes"
  review_intensity: "strict"
  team_size: "solo"
  default: "standard"
  auto_refresh: true
  session_max_messages: 50
