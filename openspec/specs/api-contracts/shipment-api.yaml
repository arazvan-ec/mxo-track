# API Contract: Shipment API
# Generated by /workflows:discover on 2026-02-19

api_contract:
  name: "Shipment API"
  version: "1.0"
  base_path: "/api/v1/shipments"
  description: "Shipment tracking and management API (V1). Customer-scoped via Doctrine tenant filter."

  endpoints:
    - method: "POST"
      path: "/api/v1/shipments"
      description: "Create single or batch shipments with validation"
      request:
        content_type: "application/json"
      responses:
        - status: 201
          description: "Shipment(s) created"
        - status: 422
          description: "Validation error"
      authentication: "session (authenticated)"

    - method: "GET"
      path: "/api/v1/shipments"
      description: "List shipments (paginated, filtered by customer tenant)"
      parameters:
        - name: "page"
          in: "query"
          type: "integer"
          required: false
        - name: "limit"
          in: "query"
          type: "integer"
          required: false
      responses:
        - status: 200
          description: "Paginated array of shipments"
      authentication: "session (authenticated)"

    - method: "GET"
      path: "/api/v1/shipments/{publicId}"
      description: "Get shipment detail"
      parameters:
        - name: "publicId"
          in: "path"
          type: "string (ULID)"
          required: true
      responses:
        - status: 200
          description: "Shipment detail"
        - status: 404
          description: "Shipment not found"
      authentication: "session (authenticated)"

    - method: "GET"
      path: "/api/v1/shipments/{publicId}/tracking"
      description: "Get full event timeline for a shipment"
      parameters:
        - name: "publicId"
          in: "path"
          type: "string (ULID)"
          required: true
      responses:
        - status: 200
          description: "Array of shipment events"
        - status: 404
          description: "Shipment not found"
      authentication: "session (authenticated)"

  source_files:
    - "backend/src/Controller/Api/V1/ShipmentApiController.php"

  extracted_at: "2026-02-19"
