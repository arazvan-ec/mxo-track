# API Contract: Shipment API
# Generated by /workflows:discover on 2026-02-19

api_contract:
  name: "Shipment API"
  version: "1.0"
  base_path: "/api/shipments"
  description: "Shipment tracking and event API. Customer-scoped via Doctrine tenant filter."

  endpoints:
    - method: "GET"
      path: "/api/shipments"
      description: "List shipments (filtered by customer tenant for ROLE_CUSTOMER/ROLE_DRIVER)"
      responses:
        - status: 200
          description: "Array of shipments"
      authentication: "session (authenticated)"

    - method: "GET"
      path: "/api/shipments/{publicId}/events"
      description: "Get events for a specific shipment"
      parameters:
        - name: "publicId"
          in: "path"
          type: "string (ULID)"
          required: true
      responses:
        - status: 200
          description: "Array of shipment events"
        - status: 404
          description: "Shipment not found"
      authentication: "session (authenticated)"

    - method: "POST"
      path: "/api/shipments/{publicId}/events"
      description: "Add a new event to a shipment"
      parameters:
        - name: "publicId"
          in: "path"
          type: "string (ULID)"
          required: true
      request:
        content_type: "application/json"
        schema:
          properties:
            event_type: { type: "string (ShipmentEventType)" }
            payload: { type: "object, optional" }
      responses:
        - status: 201
          description: "Event created"
        - status: 422
          description: "Validation error"
      authentication: "session (ROLE_ADMIN or ROLE_OPERATOR)"

  source_files:
    - "backend/src/Controller/ShipmentApiController.php"

  extracted_at: "2026-02-19"
