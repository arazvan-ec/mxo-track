# API Contract: Admin Web Routes
# Generated by /workflows:discover on 2026-02-19

api_contract:
  name: "Admin Web"
  version: "1.0"
  base_path: "/admin"
  description: "Admin dashboard and management web pages. HTML responses via Twig templates."

  endpoints:
    - method: "GET"
      path: "/admin"
      description: "Admin dashboard with fleet metrics"
      responses:
        - status: 200
          description: "HTML admin dashboard page"
      authentication: "session (ROLE_ADMIN or ROLE_OPERATOR)"

    - method: "GET"
      path: "/admin/shipments/import"
      description: "CSV import form for shipments"
      responses:
        - status: 200
          description: "HTML import form"
      authentication: "session (ROLE_ADMIN or ROLE_OPERATOR)"

    - method: "POST"
      path: "/admin/shipments/import"
      description: "Process CSV shipment import"
      request:
        content_type: "multipart/form-data"
      responses:
        - status: 302
          description: "Redirect after import"
      authentication: "session (ROLE_ADMIN or ROLE_OPERATOR)"

    - method: "GET"
      path: "/fleet/map"
      description: "Real-time fleet tracking map page"
      responses:
        - status: 200
          description: "HTML map page with Mercure SSE integration"
      authentication: "session (authenticated)"

    - method: "GET"
      path: "/"
      description: "Dashboard home page"
      responses:
        - status: 200
          description: "HTML dashboard"
      authentication: "session (authenticated)"

    - method: "GET"
      path: "/login"
      description: "Login page"
      responses:
        - status: 200
          description: "HTML login form"
      authentication: "PUBLIC_ACCESS"

  source_files:
    - "backend/src/Controller/AdminController.php"
    - "backend/src/Controller/DashboardController.php"
    - "backend/src/Controller/FleetMapController.php"
    - "backend/src/Controller/SecurityController.php"

  extracted_at: "2026-02-19"
