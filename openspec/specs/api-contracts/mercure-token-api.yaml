# API Contract: Mercure Token API
# Generated by /workflows:discover on 2026-02-19

api_contract:
  name: "Mercure Token API"
  version: "1.0"
  base_path: "/api/mercure"
  description: "Provides Mercure SSE subscriber tokens for real-time frontend updates."

  endpoints:
    - method: "GET"
      path: "/api/mercure/token"
      description: "Get a subscriber JWT for Mercure SSE connection. Sets mercureAuthorization cookie."
      parameters:
        - name: "topics"
          in: "query"
          type: "array of strings"
          required: false
          description: "Topics to subscribe to. Authorized via TopicResolver."
      responses:
        - status: 200
          description: "Returns JWT in mercureAuthorization cookie + JSON body"
          schema:
            properties:
              token: { type: "string (JWT)" }
        - status: 403
          description: "Topic not authorized for user"
      authentication: "session (authenticated)"

  schemas:
    MercureToken:
      type: object
      properties:
        token: { type: "string", description: "HS256 JWT for Mercure subscriber" }

  source_files:
    - "backend/src/Controller/MercureTokenController.php"
    - "backend/src/Service/MercureJwtFactory.php"
    - "backend/src/Security/TopicResolver.php"

  extracted_at: "2026-02-19"
