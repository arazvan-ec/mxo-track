# Entity Specification: VehiclePosition
# Generated by /workflows:discover on 2026-02-19

entity:
  name: "VehiclePosition"
  type: "entity"
  description: "Historical GPS position record for a vehicle. Ingested from Traccar via TraccarIngestionService."

  attributes:
    - name: "id"
      type: "BIGINT (auto-increment)"
      required: true
      description: "Internal primary key"
    - name: "publicId"
      type: "ULID"
      required: true
      description: "Public identifier"
    - name: "vehicle"
      type: "Vehicle (ManyToOne)"
      required: true
      description: "Vehicle this position belongs to"
    - name: "lat"
      type: "float"
      required: true
      description: "Latitude"
    - name: "lng"
      type: "float"
      required: true
      description: "Longitude"
    - name: "speed"
      type: "float"
      required: true
      validation: "defaults to 0"
      description: "Speed in knots"
    - name: "course"
      type: "float"
      required: true
      validation: "defaults to 0"
      description: "Course/heading in degrees"
    - name: "accuracy"
      type: "float"
      required: true
      validation: "defaults to 0"
      description: "GPS accuracy in meters"
    - name: "deviceTime"
      type: "DateTimeImmutable"
      required: true
      description: "Timestamp from GPS device"
    - name: "serverTime"
      type: "DateTimeImmutable"
      required: true
      description: "Timestamp when Traccar received the position"
    - name: "traccarPositionId"
      type: "integer"
      required: false
      description: "Traccar's internal position ID"

  relations:
    - target: "Vehicle"
      type: "many-to-one"
      cardinality: "1"
      description: "Parent vehicle (CASCADE on delete)"

  invariants:
    - "Unique constraint on (vehicle_id, device_time) prevents duplicate positions"

  source_files:
    - "backend/src/Entity/VehiclePosition.php"

  extracted_at: "2026-02-19"
