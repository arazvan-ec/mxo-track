# Entity Specification: VehicleLastPosition
# Generated by /workflows:discover on 2026-02-19

entity:
  name: "VehicleLastPosition"
  type: "entity"
  description: "Materialized view of the latest GPS position per vehicle. Updated by TraccarIngestionService for efficient fleet map queries."

  attributes:
    - name: "id"
      type: "BIGINT (auto-increment)"
      required: true
      description: "Internal primary key"
    - name: "publicId"
      type: "ULID"
      required: true
      description: "Public identifier"
    - name: "vehicle"
      type: "Vehicle (OneToOne)"
      required: true
      description: "Vehicle this position belongs to"
    - name: "lat"
      type: "float"
      required: true
      description: "Latitude"
    - name: "lng"
      type: "float"
      required: true
      description: "Longitude"
    - name: "speed"
      type: "float"
      required: true
      description: "Speed"
    - name: "course"
      type: "float"
      required: true
      description: "Course/heading"
    - name: "accuracy"
      type: "float"
      required: true
      description: "GPS accuracy"
    - name: "deviceTime"
      type: "DateTimeImmutable"
      required: true
      description: "Device timestamp"
    - name: "serverTime"
      type: "DateTimeImmutable"
      required: true
      description: "Server timestamp"

  relations:
    - target: "Vehicle"
      type: "one-to-one"
      cardinality: "1"
      description: "Unique constraint — one last position per vehicle"

  invariants:
    - "Exactly one record per vehicle (unique constraint on vehicle_id)"
    - "refresh() updates in place rather than creating new records"
    - "fromTelemetry() is a named constructor for initial creation"

  source_files:
    - "backend/src/Entity/VehicleLastPosition.php"

  extracted_at: "2026-02-19"
