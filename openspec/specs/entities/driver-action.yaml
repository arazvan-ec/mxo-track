# Entity Specification: DriverAction
# Generated by /workflows:discover on 2026-02-19

entity:
  name: "DriverAction"
  type: "entity"
  description: "Idempotency tracking for driver operations. Ensures deliver/exception actions are not duplicated via clientActionId."

  attributes:
    - name: "id"
      type: "BIGINT (auto-increment)"
      required: true
      description: "Internal primary key"
    - name: "publicId"
      type: "ULID"
      required: true
      description: "Public identifier"
    - name: "driver"
      type: "User (ManyToOne)"
      required: true
      description: "Driver who performed the action"
    - name: "clientActionId"
      type: "UUID"
      required: true
      description: "Client-generated UUID for idempotency"
    - name: "type"
      type: "string(30)"
      required: true
      description: "Action type (e.g., 'deliver', 'exception')"
    - name: "stop"
      type: "RouteStop (ManyToOne)"
      required: true
      description: "Route stop this action applies to"
    - name: "createdAt"
      type: "DateTimeImmutable"
      required: true
      description: "When the action was recorded"

  relations:
    - target: "User"
      type: "many-to-one"
      cardinality: "1"
      description: "Driver user"
    - target: "RouteStop"
      type: "many-to-one"
      cardinality: "1"
      description: "Target stop"

  invariants:
    - "Unique constraint on (driver_user_id, client_action_id) ensures idempotency"
    - "DriverActionService checks for existing action before recording new one"

  source_files:
    - "backend/src/Entity/DriverAction.php"
    - "backend/src/Service/DriverActionService.php"

  extracted_at: "2026-02-19"
