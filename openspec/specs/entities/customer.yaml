# Entity Specification: Customer
# Generated by /workflows:discover on 2026-02-19

entity:
  name: "Customer"
  type: "entity"
  description: "Tenant entity. Represents a logistics company/customer. Used for multi-tenant data isolation via CustomerTenantFilter."

  attributes:
    - name: "id"
      type: "BIGINT (auto-increment)"
      required: true
      description: "Internal primary key"
    - name: "publicId"
      type: "ULID"
      required: true
      description: "Public identifier"
    - name: "name"
      type: "string(150)"
      required: true
      description: "Customer/company name"

  relations:
    - target: "User"
      type: "one-to-many"
      cardinality: "0..*"
      description: "Users belonging to this customer"
    - target: "Shipment"
      type: "one-to-many"
      cardinality: "0..*"
      description: "Shipments scoped to this customer"
    - target: "CustomerVehicle"
      type: "one-to-many"
      cardinality: "0..*"
      description: "Vehicle assignments"
    - target: "CsvImportRun"
      type: "one-to-many"
      cardinality: "0..*"
      description: "CSV import history"

  invariants:
    - "Customer is the tenant root — all scoped entities reference this entity"
    - "CustomerTenantFilter uses customer_id column to isolate data"

  source_files:
    - "backend/src/Entity/Customer.php"
    - "backend/src/Doctrine/CustomerTenantFilter.php"
    - "backend/src/EventSubscriber/DoctrineCustomerFilterSubscriber.php"

  extracted_at: "2026-02-19"
