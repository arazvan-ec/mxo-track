# Entity Specification: AuditLog
# Generated by /workflows:discover on 2026-02-19

entity:
  name: "AuditLog"
  type: "entity"
  description: "Structured audit trail for security-sensitive operations. Records actor, action, target entity, and payload."

  attributes:
    - name: "id"
      type: "BIGINT (auto-increment)"
      required: true
      description: "Internal primary key"
    - name: "publicId"
      type: "ULID"
      required: true
      description: "Public identifier"
    - name: "actor"
      type: "User (ManyToOne)"
      required: true
      description: "User who performed the action"
    - name: "action"
      type: "string(80)"
      required: true
      description: "Action name (e.g., 'shipment.create', 'route.start')"
    - name: "entityType"
      type: "string(80)"
      required: true
      description: "Type of entity affected"
    - name: "entityId"
      type: "string(40)"
      required: true
      description: "Public ID of affected entity"
    - name: "payload"
      type: "json"
      required: true
      validation: "defaults to empty object"
      description: "Additional context/data for the audit entry"
    - name: "createdAt"
      type: "DateTimeImmutable"
      required: true
      description: "When the action was recorded"

  relations:
    - target: "User"
      type: "many-to-one"
      cardinality: "1"
      description: "Actor user"

  invariants:
    - "AuditLog entries are append-only (never updated or deleted)"
    - "entityId should use public ID, not internal ID"

  source_files:
    - "backend/src/Entity/AuditLog.php"
    - "backend/src/Service/AuditLogger.php"

  extracted_at: "2026-02-19"
