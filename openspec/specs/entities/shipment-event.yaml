# Entity Specification: ShipmentEvent
# Generated by /workflows:discover on 2026-02-19

entity:
  name: "ShipmentEvent"
  type: "entity"
  description: "Immutable event recording a shipment lifecycle transition. Event-sourced approach to tracking shipment status."

  attributes:
    - name: "id"
      type: "BIGINT (auto-increment)"
      required: true
      description: "Internal primary key"
    - name: "publicId"
      type: "ULID"
      required: true
      description: "Public identifier"
    - name: "shipment"
      type: "Shipment (ManyToOne)"
      required: true
      description: "Parent shipment"
    - name: "eventType"
      type: "ShipmentEventType enum"
      required: true
      validation: "CREATED|PICKED_UP|IN_HUB|IN_TRANSIT|OUT_FOR_DELIVERY|DELIVERED|EXCEPTION"
      description: "Type of lifecycle event"
    - name: "payload"
      type: "json"
      required: true
      validation: "defaults to empty array"
      description: "Event-specific data"
    - name: "createdAt"
      type: "DateTimeImmutable"
      required: true
      description: "When the event occurred"

  relations:
    - target: "Shipment"
      type: "many-to-one"
      cardinality: "1"
      description: "Parent shipment (CASCADE on delete)"

  invariants:
    - "ShipmentEvent records are append-only (immutable)"
    - "The latest event determines the current shipment status"

  source_files:
    - "backend/src/Entity/ShipmentEvent.php"
    - "backend/src/Enum/ShipmentEventType.php"

  extracted_at: "2026-02-19"
