# API Architecture Diagnostic
# Generated by /workflows:discover on 2026-02-19

diagnostic_version: "1.0"
generated_at: "2026-02-19"
generated_by: "/workflows:discover --setup"

dimensions:
  data_flow:
    value: "bidirectional"
    evidence:
      - "Controllers expose REST/Web endpoints (egress)"
      - "TraccarApiClient consumes Traccar REST API (ingress)"
      - "Mercure publishes to SSE hub (egress)"

  data_source_topology:
    value: "mixed_db_external"
    evidence:
      - "PostgreSQL 16 as primary database"
      - "Traccar API as external data source for GPS positions"
      - "Redis for session storage (not data)"
    external_sources:
      - name: "Traccar"
        type: "REST API"
        data_provided: "GPS device positions, device management"

  consumer_diversity:
    value: "single_consumer"
    evidence:
      - "No platform-specific DTOs or serialization groups detected"
      - "Single web application consuming all APIs"
      - "Driver API used by mobile but same format"

  dependency_isolation:
    value: "partially_wrapped"
    evidence:
      - "TraccarApiClient exists as adapter"
      - "No port/interface for TraccarApiClient (direct coupling to concrete class)"
      - "PodStorageInterface exists as port example for POD storage"
    violations:
      - entity: "TraccarApiClient"
        issue: "No interface/port — services depend on concrete class"

  concurrency_model:
    value: "synchronous"
    evidence:
      - "All HTTP calls to Traccar are sequential"
      - "No async patterns, promises, or concurrent request handling"
      - "PHP single-threaded execution model"

  response_customization:
    value: "uniform"
    evidence:
      - "Same JSON format for all consumers"
      - "No serialization groups or per-consumer DTOs"
      - "ApiErrorResponder provides consistent error format"

extracted_from:
  - "backend/src/Service/TraccarApiClient.php"
  - "backend/src/Controller/"
  - "backend/config/packages/"

extraction_method: "static analysis"
last_validated: "2026-02-19"
